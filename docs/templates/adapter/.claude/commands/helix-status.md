---
description: HELIX status / completion packet / doctor 出力を確認する。
---

`ut-tdd status --json`、`ut-tdd completion decision-packet --json`、`ut-tdd doctor --profile consumer` を実行し、HELIX mode、completionClaimAllowed、未完了 blocker、hard-gate failure を要約する。
