Propose a TypeScript implementation of the function clampRange(value: number, min: number, max: number): number. It must return value clamped into [min, max], and must throw a RangeError when min > max. Reply with exactly one fenced code block labeled typescript containing only the function as an export (export function clampRange...). Do not use tools, do not create or modify files, and do not add any prose outside the code block.
