# outside67 product-entry follow-up research (003/006/009/012/014)

initial requested exact base `74f724b4699feca7f9f2e87e939fe6578f75669e` から fresh isolated worktree `/home/tenni/.helix-worktrees/outside67-next-076` を作り、outside67 holding 67件のうち既済32件と重複しない5 source/path_revision_pairを静的保持するresearch Scaffoldです。初期要求base `74f724b4699feca7f9f2e87e939fe6578f75669e` からorigin/mainのmain drift（`6396443e150d285b0b08edd43ea52a26f85dd434`）を検知し、親指示でrebaseline済み。候補5件を加えた研究会計は37/67、残30ですが、これはpath_revision_pair研究会計であり正式holding採否ではありません。正式要求identityの件数、四製品ownerの割当、実装・縮退状態の分母でもありません。

候補は003 HARNESS entry README、006 HELIX-OS entry README、009 HELIX-Web-OS entry README、012 HELIX-Web entry README、014 shared Concept-derived L2 crosswalkです。各sourceから5本ずつ、合計25本のpre-isolation/archive exact line anchorとcurrent counterpartを保持します。multi-duty lineの完全分解は未了なのでcomposite_unresolvedをopen questionに残します。

003/006/009はpre/archive内容が同一ですがcurrent product-entry counterpartへ移設され内容が変化しています。012/014はpre/archiveも差分があり、current counterpartも移設・内容差分があります。hash・path・wording観測をsemantic equivalence、authority、successor、implementation、degradation、failure、consumer closure、decision closureへ変換していません。四製品はcandidate boundary only、product/phase/implementation/degradation/failure/consumer/decisionはunknownです。旧7 ledgerのcandidate ID/path exact hitは0件ですが、未調査の不在・完了・承認の証拠とは扱いません。

SCF-B-0076はresearch Scaffold Bindingとして登録済みです。今回の検証幅は5 source pairであり、安全なbatch上限は断定せず、次batchはsource chainごとに独立確認します。旧archive runtime/test/CI、現行runtime、GitHub/PR/DB操作は行っていません。

stacked stop条件: このrebaseline後にorigin/mainまたはparent lineageが進んだ場合はsource選定・正式化を停止し、最新mainへの再materializationとscope/base digestの再検証を行う。silent rebase・merge・holding昇格はしない。
