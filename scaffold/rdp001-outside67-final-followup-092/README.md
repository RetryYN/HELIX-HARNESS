# outside67 final residual source-chain research (048/049/050/051/053)

exact parent HEAD `ad11da4a85…`（#2039 merged into main at `36accee662…`）から、outside67 の 67 `path_revision_pair` のうち、SCF-B-0090 が固定した 62 件と重複しない最後の 048／049／050／051／053 を静的に保持する research Scaffold です。研究会計は `67/67`、残りは `0` 件ですが、これは path_revision_pair の調査会計であり、正式な要求identity、四製品owner、phase、実装、縮退、authority の分母ではありません。

048／050／051 は crosswalk、049 は product placement audit、053 は Concept v4.1 candidate source です。各 source pair は pre-isolation と archive の exact path／blob／bytes／SHA／line anchors、および現行 counterpart の path／blob／bytes／SHA を保持します。pre/archive/current の byte・path 関係と semantic inclusion は別観測です。

048／050／051／053 は pre/archive が different、049 は same です。current counterpart は path relation として取得し、048／051／053 は archive hash drift、049／050 は archive hash equal を観測として保持します。これは意味同値、authority、successor、実装、縮退、failure、consumer closure、decision を生成しません。四製品は候補境界だけを列挙し、product owner と phase authority は `unknown` のままです。各 line は複数義務を含み得るため、`composite_unresolved` として atomization 前の exact fragment に留めています。

旧 archive の source は意味・判断史・failure・consumer の静的参照としてのみ読みました。旧 workflow／runtime／test／CI／hook／adapter は実行していません。正式 holding admission、要求採否、実装、受入、release、deployment は行っていません。

主なファイルは `inventory.json`、`selected-source-items.jsonl`、`product-units.jsonl`、`source-diffs.json`、`evidence-scan.json`、source snapshot、`validate.py`、`selfcheck.py`、および再生成用 `generate.py` です。#2039 の parent HEAD が変わった場合は、選定・生成・commit・pushを停止してrebaselineします。
