# 調査前提

- 67件はpath_revision_pairのholding分母で、25 product-unit候補や32/67研究会計を正式要求数として扱わない。 正式要求identity、四製品owner、実装・縮退状態の分母にも扱わない。
- 四つの対象別L1とshared L2／AI-read／upstream policyは候補境界であり、assignment、owner、authority、successorを確定しない。
- pre-isolation、archive、current counterpartは独立観測であり、hash一致・path relocation・wording driftも意味同値、authority、実装、縮退、failure、consumer、decisionの証拠ではない。
- multi-duty lineは完全分解前の候補で、composite_unresolvedを残す。逐語anchor外の主体・動作・条件・否定・順序を生成しない。
- 旧assetは意味、判断史、failure、consumerを調べるreferenceとしてだけ読み、旧runtime/test/CIを実行しない。
- SCF-B-0074はresearch Scaffold Bindingとして登録済みであり、正式holding採否・PRは生成しない。
