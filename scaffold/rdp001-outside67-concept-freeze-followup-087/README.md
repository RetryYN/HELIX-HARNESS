# outside67 residual source-chain research (017/021/025/026/027)

`origin/main` `3cdde5dfe…`（#2034 merge後）から、outside67 の 67 `path_revision_pair` のうち、SCF-B-0085 が固定した 52 件と重複しない残余から 017／021／025／026／027 を静的に保持する research Scaffold です。研究会計は `57/67`、残りは `10` 件ですが、これは path_revision_pair の調査会計であり、正式な要求identity、四製品owner、phase、実装、縮退、authority の分母ではありません。

017 は Concept v4.1 の Claude 意味レビュー、021 は Infinity の機能要求対象別 crosswalk、025／026／027 は L2 freeze IR proposal の acceptance／requirement／system contract patch です。各 source pair は pre-isolation と archive の exact blob／bytes／SHA／line anchors、および現行 `source-rebaseline` counterpart の path／bytes／SHA を保持します。pre/archive/current の byte・path 関係と semantic inclusion は別観測です。

017／025／026／027 は pre/archive と archive/current が byte equal、021 は pre/archive revision と current content drift を保持します。これは意味同値、authority、successor、実装、縮退、failure、consumer closure、decision を生成しません。四製品は候補境界だけを列挙し、product owner と phase authority は `unknown` のままです。各 line は複数義務を含み得るため、`composite_unresolved` として atomization 前の exact fragment に留めています。

旧 archive の source は意味・判断史・failure・consumer の静的参照としてのみ読みました。旧 workflow／runtime／test／CI／hook／adapter は実行していません。正式 holding admission、要求採否、実装、受入、release、deployment は行っていません。

主なファイルは `inventory.json`、`selected-source-items.jsonl`、`product-units.jsonl`、`source-diffs.json`、`evidence-scan.json`、source snapshot、`validate.py`、`selfcheck.py`、および再生成用 `generate.py` です。
