#!/usr/bin/env sh
# HELIX thin POSIX entrypoint (ADR-001). source in checkout, dist in package.
set -e
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
if [ -f "$ROOT/src/cli.ts" ]; then
  exec npx --no-install tsx "$ROOT/src/cli.ts" "$@"
fi
exec "$ROOT/dist/helix" "$@"
